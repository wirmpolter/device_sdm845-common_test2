<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaomi.settings.doze"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="24"/>

    <application
        android:label="XiaomiDoze"
        android:persistent="true">

        <receiver android:name="com.xiaomi.settings.doze.BootCompletedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <service android:name="com.xiaomi.settings.doze.DozeService"
                 android:permission="XiaomiDozeService">
        </service>

        <activity
            android:name=".DozeSettings"
            android:label="@string/xiaomidoze_display_title"
            android:summary="@string/xiaomidoze_display_summary"
            android:icon="@drawable/ic_settings_doze"
            android:theme="@android:style/Theme.DeviceDefault.Settings">
            <intent-filter>
                <action android:name="com.android.settings.action.EXTRA_SETTINGS" />
            </intent-filter>
               <meta-data
                 android:name="com.android.settings.dismiss"
                 android:value="9,13,30" />
               <meta-data
                 android:name="com.android.settings.category"
                 android:value="com.android.settings.category.ia.homepage" />
               <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/xiaomidoze_display_summary" />
        </activity>

    </application>
</manifest>
